dependencies {
    api project(':gradle-base')
    api gradleTestKit()
    api libs.junit
    api libs.guava
    api (libs.spock) {
        exclude group: 'org.apache.groovy'
    }
    api libs.bundles.commons
    api libs.bundles.jetbrains
}